# Voice Feature Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add a voice narration feature — step 5 in the wizard lets Premium users pick one of 6 character voices; the story reader shows an audio player that generates and plays TTS via OpenAI.

**Architecture:** A new wizard step 5 sits between "Детали" and story generation. Free users see a freemium paywall at step 5; Premium users choose from a 2×3 grid of voice characters. After generation, a fixed AudioPlayer component in the story reader lazy-loads TTS audio (generated by a new `/api/tts` route using OpenAI TTS API) only when the user presses play.

**Tech Stack:** OpenAI TTS API (`tts-1` model), React state for voiceId, new `/api/tts/route.ts`, new `AudioPlayer.tsx` component, updates to `messages/ru.json` + `messages/en.json`, updates to `app/[locale]/page.tsx`.

---

## File Map

| File | Action | Purpose |
|------|--------|---------|
| `.env.local` | Modify | Add `OPENAI_API_KEY` |
| `messages/ru.json` | Modify | Add voice characters config + step 5 strings |
| `messages/en.json` | Modify | Same in English |
| `app/api/tts/route.ts` | Create | TTS endpoint — receives text + voiceId, calls OpenAI, streams audio |
| `components/AudioPlayer.tsx` | Create | Fixed-bottom audio player with lazy TTS generation |
| `app/[locale]/page.tsx` | Modify | FormData + step 5 UI + AudioPlayer integration |

---

## Voice Character Reference

| Name (RU) | Name (EN) | Ages | OpenAI voice | Emoji |
|-----------|-----------|------|--------------|-------|
| Сказочник | Storyteller | 5–10 лет | `fable` | 🧙‍♂️ |
| Весёлая Подружка | Cheerful Pal | 3–5 лет | `nova` | 🌸 |
| Добрый Великан | Kind Giant | 3–5 лет | `onyx` | 🐻 |
| Нолик-Умник | Little Smarty | 5–7 лет | `echo` | 🦊 |
| Леди-Храбрость | Lady Brave | 6–9 лет | `shimmer` | ⚔️ |
| Супергерой | Superhero | 7–10 лет | `alloy` | 🦸 |

---

## Task 1: Add OpenAI API key

**Files:**
- Modify: `.env.local`

- [ ] **Step 1: Add the key**

Get your key from https://platform.openai.com/api-keys and add to `.env.local`:

```bash
# .env.local — add this line
OPENAI_API_KEY=sk-proj-...your-key-here...
```

- [ ] **Step 2: Verify it's readable**

```bash
grep OPENAI /Users/igor/Projects/fairy-tale-app/.env.local
```
Expected: one line starting with `OPENAI_API_KEY=sk-`

---

## Task 2: Add voice characters to messages

**Files:**
- Modify: `messages/ru.json`
- Modify: `messages/en.json`

- [ ] **Step 1: Add voice section to `messages/ru.json`**

Inside the `"wizard"` object (after `"step4Label"` entry), add:

```json
"step5Label": "Голос сказки",
"voiceTitle": "Выбери голос сказки",
"voiceSubtitle": "Ребёнок услышит историю этим голосом",
"voiceSkip": "Без озвучки",
"voiceNext": "Далее →",
"voicePremiumTitle": "Голос для сказки",
"voicePremiumBadge": "Доступно в Premium",
"voicePremiumText": "Ребёнок услышит сказку голосом любимого персонажа",
"voicePremiumCta": "Попробовать Premium",
"voicePremiumSkip": "Создать без озвучки →",
"voices": [
  { "id": "skazochnik", "openaiVoice": "fable",   "emoji": "🧙", "name": "Сказочник",       "ages": "5–10 лет", "hint": "Добрый, спокойный голос" },
  { "id": "podrugza",   "openaiVoice": "nova",    "emoji": "🌸", "name": "Весёлая Подружка","ages": "3–5 лет",  "hint": "Звонкий, весёлый голос" },
  { "id": "velikan",    "openaiVoice": "onyx",    "emoji": "🐻", "name": "Добрый Великан",  "ages": "3–5 лет",  "hint": "Глубокий, уютный голос" },
  { "id": "umnik",      "openaiVoice": "echo",    "emoji": "🦊", "name": "Нолик-Умник",     "ages": "5–7 лет",  "hint": "Чёткий, любознательный" },
  { "id": "hrabrost",   "openaiVoice": "shimmer", "emoji": "⚔️", "name": "Леди-Храбрость",  "ages": "6–9 лет",  "hint": "Уверенный, благородный" },
  { "id": "superhero",  "openaiVoice": "alloy",   "emoji": "🦸", "name": "Супергерой",      "ages": "7–10 лет", "hint": "Энергичный, бодрый голос" }
]
```

Also update `"stepLabel"` to support variable total:
```json
"stepLabel": "Шаг {step} из {total} — {label}",
```

- [ ] **Step 2: Add voice section to `messages/en.json`**

Inside the `"wizard"` object:

```json
"step5Label": "Story Voice",
"voiceTitle": "Choose a story voice",
"voiceSubtitle": "Your child will hear the story in this voice",
"voiceSkip": "No narration",
"voiceNext": "Next →",
"voicePremiumTitle": "Voice for the story",
"voicePremiumBadge": "Available in Premium",
"voicePremiumText": "Your child will hear the story in the voice of their favourite character",
"voicePremiumCta": "Try Premium",
"voicePremiumSkip": "Create without narration →",
"voices": [
  { "id": "skazochnik", "openaiVoice": "fable",   "emoji": "🧙", "name": "Storyteller",    "ages": "5–10 y",  "hint": "Warm, calm narrator" },
  { "id": "podrugza",   "openaiVoice": "nova",    "emoji": "🌸", "name": "Cheerful Pal",   "ages": "3–5 y",   "hint": "Bright, playful voice" },
  { "id": "velikan",    "openaiVoice": "onyx",    "emoji": "🐻", "name": "Kind Giant",     "ages": "3–5 y",   "hint": "Deep, cosy voice" },
  { "id": "umnik",      "openaiVoice": "echo",    "emoji": "🦊", "name": "Little Smarty",  "ages": "5–7 y",   "hint": "Clear, curious voice" },
  { "id": "hrabrost",   "openaiVoice": "shimmer", "emoji": "⚔️", "name": "Lady Brave",     "ages": "6–9 y",   "hint": "Confident, noble voice" },
  { "id": "superhero",  "openaiVoice": "alloy",   "emoji": "🦸", "name": "Superhero",      "ages": "7–10 y",  "hint": "Energetic, bold voice" }
]
```

Also update `"stepLabel"`:
```json
"stepLabel": "Step {step} of {total} — {label}",
```

- [ ] **Step 3: Commit**

```bash
git add messages/ru.json messages/en.json
git commit -m "feat: add voice characters config to messages"
```

---

## Task 3: Create `/api/tts` route

**Files:**
- Create: `app/api/tts/route.ts`

This endpoint takes the full story text + an OpenAI voice name, calls `openai.audio.speech.create()`, and streams the MP3 back to the client.

- [ ] **Step 1: Install OpenAI SDK**

```bash
cd /Users/igor/Projects/fairy-tale-app && npm install openai
```

Expected: `added 1 package` (or already present)

- [ ] **Step 2: Create the route**

Create `app/api/tts/route.ts`:

```typescript
import { NextRequest, NextResponse } from 'next/server'
import OpenAI from 'openai'

export const maxDuration = 60

// Valid OpenAI TTS voices
const ALLOWED_VOICES = ['alloy', 'echo', 'fable', 'onyx', 'nova', 'shimmer'] as const
type AllowedVoice = typeof ALLOWED_VOICES[number]

function isAllowedVoice(v: string): v is AllowedVoice {
  return ALLOWED_VOICES.includes(v as AllowedVoice)
}

export async function POST(req: NextRequest) {
  const apiKey = process.env.OPENAI_API_KEY
  if (!apiKey) {
    return NextResponse.json({ error: 'OPENAI_API_KEY not configured' }, { status: 500 })
  }

  let text: string
  let voiceName: string

  try {
    const body = await req.json()
    text = body.text
    voiceName = body.voice
  } catch {
    return NextResponse.json({ error: 'Invalid request body' }, { status: 400 })
  }

  if (!text || typeof text !== 'string' || text.trim().length === 0) {
    return NextResponse.json({ error: 'text is required' }, { status: 400 })
  }
  if (!voiceName || !isAllowedVoice(voiceName)) {
    return NextResponse.json({ error: 'Invalid voice' }, { status: 400 })
  }

  // Truncate to ~4000 chars to avoid huge bills on abuse
  const truncatedText = text.slice(0, 4000)

  const client = new OpenAI({ apiKey })

  try {
    const mp3 = await client.audio.speech.create({
      model: 'tts-1',
      voice: voiceName,
      input: truncatedText,
      response_format: 'mp3',
    })

    const buffer = Buffer.from(await mp3.arrayBuffer())

    return new NextResponse(buffer, {
      headers: {
        'Content-Type': 'audio/mpeg',
        'Content-Length': String(buffer.length),
        'Cache-Control': 'private, max-age=3600',
      },
    })
  } catch (err: unknown) {
    const msg = err instanceof Error ? err.message : 'TTS error'
    console.error('OpenAI TTS error:', msg)
    return NextResponse.json({ error: msg }, { status: 500 })
  }
}
```

- [ ] **Step 3: Smoke-test manually (after dev server is running)**

```bash
curl -X POST http://localhost:3000/api/tts \
  -H "Content-Type: application/json" \
  -d '{"text":"Жили-были лисёнок и медвежонок.","voice":"fable"}' \
  --output /tmp/test.mp3 -w "%{http_code}"
```

Expected: `200` and `/tmp/test.mp3` is a non-empty file you can open in QuickTime.

- [ ] **Step 4: Commit**

```bash
git add app/api/tts/route.ts package.json package-lock.json
git commit -m "feat: add /api/tts route for OpenAI TTS narration"
```

---

## Task 4: Create AudioPlayer component

**Files:**
- Create: `components/AudioPlayer.tsx`

Fixed-bottom player. Lazy: fetches audio only on first play. States: `idle` → `loading` → `ready` → `playing`/`paused` → `error`.

- [ ] **Step 1: Create the component**

Create `components/AudioPlayer.tsx`:

```tsx
'use client'

import { useState, useRef, useEffect, useCallback } from 'react'

interface VoiceCharacter {
  id: string
  openaiVoice: string
  emoji: string
  name: string
  ages: string
}

interface AudioPlayerProps {
  storyText: string          // Full concatenated story text
  voice: VoiceCharacter
}

type PlayerState = 'idle' | 'loading' | 'ready' | 'playing' | 'paused' | 'error'

export function AudioPlayer({ storyText, voice }: AudioPlayerProps) {
  const [state, setState] = useState<PlayerState>('idle')
  const [progress, setProgress] = useState(0)          // 0–1
  const [duration, setDuration] = useState(0)
  const [errorMsg, setErrorMsg] = useState('')
  const audioRef = useRef<HTMLAudioElement | null>(null)
  const audioBlobUrl = useRef<string | null>(null)

  // Cleanup blob URL on unmount
  useEffect(() => {
    return () => {
      if (audioBlobUrl.current) URL.revokeObjectURL(audioBlobUrl.current)
    }
  }, [])

  const loadAudio = useCallback(async () => {
    if (audioBlobUrl.current) return  // Already loaded

    setState('loading')
    setErrorMsg('')

    try {
      const res = await fetch('/api/tts', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ text: storyText, voice: voice.openaiVoice }),
      })

      if (!res.ok) {
        const err = await res.json().catch(() => ({ error: 'Ошибка озвучки' }))
        throw new Error(err.error || `HTTP ${res.status}`)
      }

      const blob = await res.blob()
      const url = URL.createObjectURL(blob)
      audioBlobUrl.current = url

      const audio = new Audio(url)
      audioRef.current = audio

      audio.addEventListener('loadedmetadata', () => {
        setDuration(audio.duration)
        setState('ready')
      })
      audio.addEventListener('timeupdate', () => {
        if (audio.duration > 0) setProgress(audio.currentTime / audio.duration)
      })
      audio.addEventListener('ended', () => {
        setState('paused')
        setProgress(0)
        audio.currentTime = 0
      })
      audio.addEventListener('error', () => {
        setState('error')
        setErrorMsg('Ошибка воспроизведения')
      })

      await new Promise<void>((resolve, reject) => {
        audio.addEventListener('canplaythrough', () => resolve(), { once: true })
        audio.addEventListener('error', () => reject(new Error('Audio load failed')), { once: true })
        audio.load()
      })

      setState('ready')
    } catch (err) {
      setState('error')
      setErrorMsg(err instanceof Error ? err.message : 'Ошибка озвучки')
    }
  }, [storyText, voice.openaiVoice])

  const handlePlay = async () => {
    if (state === 'idle' || state === 'error') {
      await loadAudio()
      if (audioRef.current && state !== 'error') {
        await audioRef.current.play()
        setState('playing')
      }
      return
    }
    if (state === 'loading') return
    if (!audioRef.current) return

    if (state === 'ready' || state === 'paused') {
      await audioRef.current.play()
      setState('playing')
    } else if (state === 'playing') {
      audioRef.current.pause()
      setState('paused')
    }
  }

  const handleSeek = (e: React.MouseEvent<HTMLDivElement>) => {
    if (!audioRef.current || duration === 0) return
    const rect = e.currentTarget.getBoundingClientRect()
    const ratio = (e.clientX - rect.left) / rect.width
    audioRef.current.currentTime = ratio * duration
    setProgress(ratio)
  }

  const formatTime = (secs: number) => {
    const m = Math.floor(secs / 60)
    const s = Math.floor(secs % 60)
    return `${m}:${s.toString().padStart(2, '0')}`
  }

  const currentTime = audioRef.current ? audioRef.current.currentTime : 0
  const isLoading = state === 'loading'
  const isPlaying = state === 'playing'
  const hasError = state === 'error'

  return (
    <div
      className="fixed bottom-0 left-0 right-0 z-30"
      style={{
        background: 'rgba(13, 43, 30, 0.97)',
        backdropFilter: 'blur(16px)',
        borderTop: '1px solid rgba(255,255,255,0.08)',
        boxShadow: '0 -8px 32px rgba(0,0,0,0.3)',
      }}
    >
      <div className="max-w-[680px] mx-auto px-4 py-3">
        {/* Progress bar */}
        {(state === 'ready' || state === 'playing' || state === 'paused') && (
          <div
            className="w-full h-1 rounded-full mb-3 cursor-pointer"
            style={{ background: 'rgba(255,255,255,0.12)' }}
            onClick={handleSeek}
          >
            <div
              className="h-full rounded-full transition-all"
              style={{ width: `${progress * 100}%`, background: '#a46713' }}
            />
          </div>
        )}

        <div className="flex items-center gap-3">
          {/* Character avatar */}
          <div
            className="w-9 h-9 rounded-full flex items-center justify-center text-lg flex-shrink-0"
            style={{ background: 'rgba(255,255,255,0.08)', border: '1.5px solid rgba(255,255,255,0.15)' }}
          >
            {voice.emoji}
          </div>

          {/* Info */}
          <div className="flex-1 min-w-0">
            <div className="text-xs font-semibold truncate" style={{ color: '#d4a85a' }}>
              {voice.name}
            </div>
            {hasError ? (
              <div className="text-xs" style={{ color: '#ef4444' }}>{errorMsg}</div>
            ) : (state === 'ready' || state === 'playing' || state === 'paused') ? (
              <div className="text-xs" style={{ color: 'rgba(255,255,255,0.35)' }}>
                {formatTime(currentTime)} / {formatTime(duration)}
              </div>
            ) : (
              <div className="text-xs" style={{ color: 'rgba(255,255,255,0.35)' }}>
                {isLoading ? 'Готовим озвучку...' : 'Нажми ▶ чтобы слушать'}
              </div>
            )}
          </div>

          {/* Play/Pause button */}
          <button
            onClick={handlePlay}
            disabled={isLoading}
            className="w-10 h-10 rounded-full flex items-center justify-center flex-shrink-0 transition-all cursor-pointer disabled:opacity-50"
            style={{ background: isLoading ? 'rgba(255,255,255,0.1)' : '#a46713' }}
            aria-label={isPlaying ? 'Пауза' : 'Играть'}
          >
            {isLoading ? (
              /* Spinner */
              <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="white" strokeWidth="2" className="animate-spin">
                <circle cx="12" cy="12" r="10" strokeOpacity="0.3"/>
                <path d="M12 2a10 10 0 0 1 10 10" strokeLinecap="round"/>
              </svg>
            ) : isPlaying ? (
              /* Pause icon */
              <svg width="18" height="18" viewBox="0 0 24 24" fill="white">
                <rect x="6" y="4" width="4" height="16" rx="1"/>
                <rect x="14" y="4" width="4" height="16" rx="1"/>
              </svg>
            ) : (
              /* Play icon */
              <svg width="18" height="18" viewBox="0 0 24 24" fill="white">
                <polygon points="5,3 19,12 5,21"/>
              </svg>
            )}
          </button>
        </div>
      </div>
    </div>
  )
}
```

- [ ] **Step 2: Verify TypeScript compiles**

```bash
cd /Users/igor/Projects/fairy-tale-app && npx tsc --noEmit 2>&1 | head -20
```

Expected: no errors (or only pre-existing errors unrelated to this file).

- [ ] **Step 3: Commit**

```bash
git add components/AudioPlayer.tsx
git commit -m "feat: add AudioPlayer component for TTS narration"
```

---

## Task 5: Update wizard in page.tsx

**Files:**
- Modify: `app/[locale]/page.tsx`

Five changes:
1. Add `voiceId?: string` to `FormData` interface
2. Update `TOTAL_STEPS` and `STEP_LABELS` to 5
3. Add step 5 voice selection JSX (freemium or premium grid)
4. Update navigation buttons (`step<4` → `step<5`, etc.)
5. Add `selectedVoice` state at App level; wire through `generate()`

- [ ] **Step 1: Define VoiceCharacter type and update FormData**

In `app/[locale]/page.tsx`, find the `// ── Types ──` block (around line 42).

After `interface Scene { ... }` add:
```typescript
interface VoiceCharacter { id:string; openaiVoice:string; emoji:string; name:string; ages:string; hint?:string }
```

Update `FormData` interface — add the optional `voice` object (not just an id — we need the full object for the audio player):
```typescript
interface FormData { childName:string; age:string; hero:string; situation:string; situationType:SituationType; favorites:string; lesson:string; voice?:VoiceCharacter }
```

- [ ] **Step 2: Add `TOTAL_STEPS` constant and update STEP_LABELS**

Find (around line 534):
```typescript
const STEP_LABELS = [t('wizard.step1Label'),t('wizard.step2Label'),t('wizard.step3Label'),t('wizard.step4Label')]
```

Replace with:
```typescript
const TOTAL_STEPS = 5
const STEP_LABELS = [t('wizard.step1Label'),t('wizard.step2Label'),t('wizard.step3Label'),t('wizard.step4Label'),t('wizard.step5Label')]
```

- [ ] **Step 3: Update stepLabel render to pass `total`**

Find (around line 719):
```typescript
{t('wizard.stepLabel', {step, label: STEP_LABELS[step-1]})}{step===4?` ${t('wizard.optional')}`:''}
```

Replace with:
```typescript
{t('wizard.stepLabel', {step, total: TOTAL_STEPS, label: STEP_LABELS[step-1]})}{step===4?` ${t('wizard.optional')}`:''}
```

- [ ] **Step 4: Add voice characters data and step 5 JSX**

Inside `CreateForm`, after where `FAVORITES` is read (around line 495), add:

```typescript
const VOICES: VoiceCharacter[] = tRaw('wizard.voices')
```

Then find the end of step 4 JSX block (around line 835, just before `{/* Navigation */}`). Insert the step 5 block **before** the `{/* Navigation */}` comment:

```tsx
{/* ── STEP 5: Выбор голоса ── */}
{step===5&&(
  isPremium() ? (
    <div className="flex flex-col gap-4">
      <div>
        <p className="text-xs font-semibold uppercase tracking-widest mb-0.5" style={{color:'#466252'}}>{t('wizard.voiceTitle')}</p>
        <p className="text-xs" style={{color:'rgba(70,98,82,0.6)'}}>{t('wizard.voiceSubtitle')}</p>
      </div>
      <div className="grid grid-cols-2 gap-3">
        {VOICES.map(v=>(
          <button
            key={v.id}
            type="button"
            onClick={()=>setForm(f=>({...f, voice:v}))}
            className="relative rounded-2xl p-3 text-left transition-all cursor-pointer"
            style={{
              background: form.voice?.id===v.id ? 'rgba(164,103,19,0.18)' : 'rgba(255,255,255,0.04)',
              border: form.voice?.id===v.id ? '2px solid #a46713' : '2px solid rgba(255,255,255,0.08)',
            }}
          >
            {form.voice?.id===v.id&&(
              <span className="absolute top-2 right-2 text-[10px] font-bold px-1.5 py-0.5 rounded-full"
                style={{background:'#a46713',color:'#fff'}}>✓</span>
            )}
            <div className="text-2xl mb-2">{v.emoji}</div>
            <div className="font-bold text-sm leading-tight mb-0.5" style={{color:'#e8d9b5'}}>{v.name}</div>
            <div className="text-[10px] font-semibold px-1.5 py-0.5 rounded-full inline-block mb-1"
              style={{background:'rgba(164,103,19,0.3)',color:'#d4a85a'}}>{v.ages}</div>
            {v.hint&&<p className="text-[10px] leading-tight" style={{color:'rgba(232,217,181,0.45)'}}>{v.hint}</p>}
          </button>
        ))}
      </div>
    </div>
  ) : (
    /* Freemium hook for Free users */
    <div className="flex flex-col items-center text-center gap-4 py-4">
      <div className="w-14 h-14 rounded-full flex items-center justify-center text-3xl"
        style={{background:'rgba(164,103,19,0.15)',border:'2px solid rgba(164,103,19,0.3)'}}>
        🎙
      </div>
      <div>
        <p className="text-xs font-bold uppercase tracking-widest mb-1" style={{color:'#a46713'}}>{t('wizard.voicePremiumBadge')}</p>
        <h3 className="font-bold text-lg mb-1" style={{color:'#e8d9b5'}}>{t('wizard.voicePremiumTitle')}</h3>
        <p className="text-sm leading-relaxed max-w-xs mx-auto" style={{color:'rgba(232,217,181,0.6)'}}>{t('wizard.voicePremiumText')}</p>
      </div>
      {/* Blurred voice grid preview */}
      <div className="relative w-full">
        <div className="grid grid-cols-2 gap-3 blur-sm opacity-50 pointer-events-none">
          {VOICES.slice(0,4).map(v=>(
            <div key={v.id} className="rounded-2xl p-3"
              style={{background:'rgba(255,255,255,0.04)',border:'2px solid rgba(255,255,255,0.08)'}}>
              <div className="text-2xl mb-2">{v.emoji}</div>
              <div className="font-bold text-sm" style={{color:'#e8d9b5'}}>{v.name}</div>
            </div>
          ))}
        </div>
        <div className="absolute inset-0 flex items-center justify-center">
          <div className="text-5xl opacity-60">🔒</div>
        </div>
      </div>
      <button
        type="button"
        onClick={()=>{ /* wired in Task 7 */ }}
        className="w-full py-3 rounded-xl font-bold text-sm cursor-pointer transition-all hover:opacity-90"
        style={{background:'#a46713',color:'#fff'}}>
        {t('wizard.voicePremiumCta')}
      </button>
    </div>
  )
)}
```

- [ ] **Step 5: Update navigation buttons**

Find (around line 846):
```typescript
{step<4&&(
  <button type="button" onClick={()=>setStep(s=>s+1)} disabled={!canNext()}
```

Replace `step<4` with `step<5`:
```typescript
{step<5&&(
  <button type="button" onClick={()=>setStep(s=>s+1)} disabled={!canNext()}
```

Find the generate-button condition (around line 853):
```typescript
{step===4&&(
  <button type="button" onClick={()=>{saveLastChild(form.childName,form.age);onGenerate(form)}} disabled={isLoading}
```

Replace `step===4` with `step===5`:
```typescript
{step===5&&(
  <button type="button" onClick={()=>{saveLastChild(form.childName,form.age);onGenerate(form)}} disabled={isLoading}
```

Find the skip button (around line 861):
```typescript
{step===4&&(
  <button type="button" onClick={()=>onGenerate(form)} disabled={isLoading}
```

Replace `step===4` with `step===5`:
```typescript
{step===5&&(
  <button type="button" onClick={()=>onGenerate(form)} disabled={isLoading}
```

- [ ] **Step 6: Add `selectedVoice` state at App level and pass to StoryReading**

`FormData.voice` carries the `VoiceCharacter` object from the wizard. In the App component we store it in a separate state so `StoryReading` can read it after the wizard is done.

**6a — Add state.** In the main App component (around line 1281 where `const [status,setStatus]` is), add:

```typescript
const [selectedVoice, setSelectedVoice] = useState<VoiceCharacter | null>(null)
```

**6b — Capture on generate.** Inside `generate()`, find the line `setStatus('done')` (around line 1468). Directly after it add:

```typescript
setSelectedVoice(data.voice ?? null)
```

**6c — Clear on openSaved.** Find `openSaved` (around line 1526). At the top of its body add:

```typescript
setSelectedVoice(null)  // saved stories have no session voice
```

**6d — Update StoryReading signature.** Find the function definition (around line 880):

```typescript
function StoryReading({story,onBack,onSave,alreadySaved,onShare,shareStatus,onDownloadPDF,pdfLoading,pdfError,storyRef,imageCache}:{
  story:Story;onBack:()=>void;onSave:()=>void;alreadySaved:boolean
  onShare:()=>void;shareStatus:string;onDownloadPDF:()=>void;pdfLoading:boolean;pdfError:string
  storyRef:React.RefObject<HTMLDivElement|null>;imageCache?:Record<number,string>
})
```

Replace with:

```typescript
function StoryReading({story,onBack,onSave,alreadySaved,onShare,shareStatus,onDownloadPDF,pdfLoading,pdfError,storyRef,imageCache,voice}:{
  story:Story;onBack:()=>void;onSave:()=>void;alreadySaved:boolean
  onShare:()=>void;shareStatus:string;onDownloadPDF:()=>void;pdfLoading:boolean;pdfError:string
  storyRef:React.RefObject<HTMLDivElement|null>;imageCache?:Record<number,string>
  voice?:VoiceCharacter|null
})
```

**6e — Pass voice to StoryReading.** Find the `<StoryReading` JSX (around line 1614). Add `voice={selectedVoice}`:

```tsx
<StoryReading
  story={story} storyRef={storyRef}
  imageCache={imageCache}
  voice={selectedVoice}
  onBack={()=>{
    Object.values(imageCache).forEach(u=>URL.revokeObjectURL(u))
    setImageCache({})
    setStatus('idle');setStory(null)
  }}
  onSave={handleSave} alreadySaved={alreadySaved}
  onShare={handleShare} shareStatus={shareStatus}
  onDownloadPDF={handleDownloadPDF} pdfLoading={pdfLoading} pdfError={pdfError}
/>
```

- [ ] **Step 7: Verify dev build compiles**

```bash
cd /Users/igor/Projects/fairy-tale-app && npm run build 2>&1 | tail -20
```

Expected: successful build or TypeScript errors only from this feature (fix them before committing).

- [ ] **Step 8: Commit**

```bash
git add app/[locale]/page.tsx
git commit -m "feat: add voice selection step 5 to wizard"
```

---

## Task 6: Wire AudioPlayer into StoryReading

**Files:**
- Modify: `app/[locale]/page.tsx` (StoryReading function)

- [ ] **Step 1: Import AudioPlayer at top of page.tsx**

Find the existing imports at the top of `app/[locale]/page.tsx`. Add:
```typescript
import { AudioPlayer } from '@/components/AudioPlayer'
```

- [ ] **Step 2: Add AudioPlayer to StoryReading return**

In `StoryReading`, find the closing `</div>` of the main reading container (the `<div className="min-h-screen bg-white...">` wrapper). The very last rendered element before the closing tag.

Find the last `</div>` that closes `<div className="min-h-screen bg-white print:bg-white"`. Before it, add:

```tsx
{/* ── Audio player (only when voice was selected) ── */}
{voice && (
  <AudioPlayer
    storyText={story.scenes.map(s => s.text).join('\n\n')}
    voice={voice}
  />
)}
```

Also add bottom padding to the story container when a player is present, so text isn't hidden behind the player. Find `<div ref={storyRef} className="max-w-[680px] mx-auto pb-10">` and change to:
```tsx
<div ref={storyRef} className={`max-w-[680px] mx-auto ${voice ? 'pb-28' : 'pb-10'}`}>
```

- [ ] **Step 3: Verify dev server**

```bash
cd /Users/igor/Projects/fairy-tale-app && npm run dev
```

1. Open http://localhost:3000
2. Fill wizard steps 1–4
3. At step 5 (as Premium/dev user): select "Сказочник"
4. Click "✨ Создать сказку"
5. After generation: verify dark player bar appears at bottom
6. Click ▶: verify spinner appears, then audio plays

- [ ] **Step 4: Commit**

```bash
git add app/[locale]/page.tsx components/AudioPlayer.tsx
git commit -m "feat: wire AudioPlayer into story reader"
```

---

## Task 7: Handle freemium paywall action in step 5

**Files:**
- Modify: `app/[locale]/page.tsx` (CreateForm, step 5 freemium block)

Currently the "Попробовать Premium" button in step 5 has a TODO comment. Wire it to the existing paywall.

- [ ] **Step 1: Pass `onShowPaywall` into CreateForm**

Find the `CreateForm` function signature (around line 478):
```typescript
function CreateForm({onGenerate,isLoading,onOpenLibrary,onShowAuth,onShowProfile,user}:{...})
```

Add `onShowPaywall?:()=>void`:
```typescript
function CreateForm({onGenerate,isLoading,onOpenLibrary,onShowAuth,onShowProfile,user,onShowPaywall}:{onGenerate:(f:FormData)=>Promise<void>;isLoading:boolean;onOpenLibrary?:()=>void;onShowAuth?:()=>void;onShowProfile?:()=>void;onShowPaywall?:()=>void;user?:User|null})
```

- [ ] **Step 2: Replace TODO in freemium button**

Find in step 5 freemium block:
```typescript
onClick={()=>{ /* TODO: open paywall */ }}
```

Replace with:
```typescript
onClick={()=>onShowPaywall?.()}
```

- [ ] **Step 3: Pass `onShowPaywall` from both CreateForm usages in App**

Find both occurrences of `<CreateForm` (around lines 1582 and 1602). Add `onShowPaywall={()=>setShowPaywall(true)}` to both:

```tsx
<CreateForm onGenerate={generate} isLoading={false} onOpenLibrary={()=>setMobileTab('library')}
  onShowAuth={()=>setShowAuth(true)} onShowProfile={()=>setShowProfile(true)}
  onShowPaywall={()=>setShowPaywall(true)} user={user}/>
```

```tsx
<CreateForm onGenerate={generate} isLoading={false} onOpenLibrary={undefined}
  onShowAuth={()=>setShowAuth(true)} onShowProfile={()=>setShowProfile(true)}
  onShowPaywall={()=>setShowPaywall(true)} user={user}/>
```

- [ ] **Step 4: Test as free user**

In browser, open DevTools → Application → Local Storage → clear `ft-paid-until`.
Navigate to step 5. Verify:
- Blurred grid appears
- "Попробовать Premium" button opens the paywall modal
- "Создать без озвучки →" skip link generates story without audio player

- [ ] **Step 5: Commit**

```bash
git add app/[locale]/page.tsx
git commit -m "feat: wire paywall action in voice step freemium hook"
```

---

## Task 8: QA checklist

Run through all scenarios manually on localhost:

- [ ] **Premium user, voice selected → story generates → audio player appears → plays correctly**
- [ ] **Premium user, no voice selected (skip) → story generates → no audio player**  
- [ ] **Free user at step 5 → sees freemium hook → paywall opens on CTA click**
- [ ] **Free user → "Создать без озвучки" → story generates without player**
- [ ] **Open saved story → no audio player (voice is session-only)**
- [ ] **Audio player seek bar → clicking halfway seeks to correct position**
- [ ] **Audio plays to end → resets to start, shows ▶ again**
- [ ] **Back navigation through all 5 steps works correctly**
- [ ] **Step counter shows "Шаг X из 5" at each step**
- [ ] **English locale: step 5 strings in English**

---

## Notes

- **Cost estimate:** OpenAI TTS-1 costs ~$15/1M characters. A 600-word story ≈ 3600 chars ≈ $0.054 per audio generation. Budget ~5 cents per Premium user per story.
- **No server-side caching in MVP** — audio generated fresh each play. User's browser caches via blob URL for session duration.
- **Character names in UI** come from `messages/*.json` (locale-aware). `openaiVoice` field in JSON is the source of truth for which voice to use.
- **Future:** Add Supabase Storage caching (store generated MP3 keyed by `storyId+voiceId`), add "Мои записанные голоса" cloning via XTTS-v2.
